#include <iostream>
using namespace std;
int main() {
int* Mas;
    int N;
   cout<<"Imput N ";
    cin>>N;
    Mas=new int[N];
    for(int i=0; i<N; ++i)
      cin>>Mas[i];
    for(int i=N-1; i>=0; --i)
        cout<<Mas[i]<<"";
    delete[] Mas;
 
}
